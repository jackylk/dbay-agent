apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dbay-agent.fullname" . }}-config
  labels:
    {{- include "dbay-agent.labels" . | nindent 4 }}
data:
  LAKEBASE_API_BASE_URL: {{ .Values.lakebase.apiBaseUrl | quote }}
